# Ignore everything inside the data and logs folders
data/*
logs/*

# But keep the folders themselves (by not ignoring .gitkeep files)
!data/.gitkeep
!logs/.gitkeep

# Ignore compiled executables if you prefer people to build their own
dist/*.exe